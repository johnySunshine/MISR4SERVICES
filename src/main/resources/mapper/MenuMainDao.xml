<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soecode.osc.dao.MenuMainDao">
    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="queryDao" resultType="Menu">
        <!-- 具体的sql -->
        SELECT
        *
        FROM
        tmenus
    </select>

    <insert id="insertDao">
        <!-- ignore 主键冲突，报错 -->
        INSERT
        ignore
        INTO
        tmenus (menutext, menuurl,target,subid,hubisvisible)
        VALUES
        (#{menuText}, #{menuUrl},#{target},#{subid},#{hubIsVisible})
    </insert>
    <update id="updateDao">
        UPDATE
        tmenus
        SET
        menutext =#{menuText},menuurl =#{menuUrl},target=#{target},subid=#{subid},hubisvisible =#{hubIsVisible}
        WHERE
        id=#{id}
    </update>


    <delete id="deleteDao">
        DELETE
        FROM
        tmenus
        WHERE
        id = #{menuId}
    </delete>
</mapper>