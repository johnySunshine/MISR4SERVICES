<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.msir.dao.ImagesDao">
    <insert id="insertDao">
        <!-- ignore 主键冲突，报错 -->
        INSERT ignore
        INTO timages
        (href, image_type,movie_id)
        VALUES
        (#{href}, #{imageType},#{movieId})
    </insert>

</mapper>